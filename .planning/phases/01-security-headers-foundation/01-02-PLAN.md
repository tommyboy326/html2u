---
phase: 01-security-headers-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/schema.sql
autonomous: false
requirements: [SEC-OPS-01]
user_setup:
  - service: supabase
    why: "pg_cron extension + cron.schedule jobs must be applied to the live production Supabase project; build/type checks pass WITHOUT this, so it is a manual gate to avoid a false-positive verification"
    dashboard_config:
      - task: "Run the updated supabase/schema.sql in the Supabase SQL editor (Dashboard → SQL → New query), which enables pg_cron and registers the three cron.schedule jobs; then confirm under Database → Extensions that pg_cron is enabled and the three jobs appear in cron.job"
        location: "Supabase Dashboard → SQL editor + Database → Extensions"
must_haves:
  truths:
    - "schema.sql commits create extension pg_cron and three cron.schedule jobs: cleanup_shares, cleanup_rate_limits, cleanup_csp_violations"
    - "schema.sql creates a forward-compatible csp_violations table so the csp_violations cleanup job references a real table and does not error on each run"
    - "The previously-commented pg_cron block is now live (uncommented) in schema.sql"
    - "An operator has applied schema.sql to the production Supabase project and confirmed pg_cron is enabled with all three jobs registered"
  artifacts:
    - path: "supabase/schema.sql"
      provides: "Live pg_cron extension + three cleanup cron jobs + minimal csp_violations table DDL"
      contains: "cron.schedule"
  key_links:
    - from: "cron.schedule('cleanup_csp_violations', ...)"
      to: "public.csp_violations table"
      via: "delete from public.csp_violations where created_at < now() - interval '30 days'"
      pattern: "csp_violations"
---

<objective>
Enable Supabase `pg_cron` in production and commit the three `cron.schedule` cleanup jobs in `supabase/schema.sql` — for `shares`, `rate_limits`, and `csp_violations`. Because the `csp_violations` table is not created until SEC-03 (a later phase) but the cron job must reference a real table to avoid erroring on every run, this plan ALSO creates a minimal, forward-compatible `csp_violations` table now.

Purpose: SEC-OPS-01. Downstream SEC-04 (Phase 3) legal pages can only TRUTHFULLY promise "30-day retention" if pg_cron actually deletes aged rows. This plan makes that promise real. The schema file is the source of truth, but enabling pg_cron and registering jobs requires applying SQL to the live Supabase project — a manual ops step that build/type checks cannot verify.

Output: An updated `supabase/schema.sql` (uncommented + extended pg_cron block + new csp_violations table). No code files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md

<interfaces>
<!-- Current schema.sql tail (lines ~111-119): commented pg_cron block has ONLY shares + rate_limits, NOT csp_violations. -->
From supabase/schema.sql:
- Tables: public.shares (has created_at, expires_at), public.rate_limits (has expires_at). RLS enabled, no policies (service_role bypasses).
- The commented block:
  `-- create extension if not exists pg_cron;`
  `-- select cron.schedule('cleanup_shares', '0 * * * *', $$delete from public.shares where expires_at < now()$$);`
  `-- select cron.schedule('cleanup_rate_limits', '*/30 * * * *', $$delete from public.rate_limits where expires_at < now()$$);`
- Note: NO csp_violations job in the existing block. Do NOT just uncomment — you must ADD the csp_violations table + its cron job.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add forward-compatible csp_violations table and live pg_cron block to schema.sql</name>
  <files>supabase/schema.sql</files>
  <read_first>
    - supabase/schema.sql (the file being modified — note the existing table DDL style: `create table if not exists`, RLS-enable pattern, and the commented pg_cron block at lines ~111-119 that has only shares + rate_limits)
    - .planning/REQUIREMENTS.md (SEC-OPS-01 for the exact three delete statements; SEC-03 for the eventual csp_violations dedup shape so this table is forward-compatible: deduped by (document_uri_path, directive, blocked_host, source_host) with a count column and a created_at)
  </read_first>
  <action>
    DECISION (Tension 2): Create a minimal `csp_violations` table NOW rather than deferring the cron job. Rationale to record in the SUMMARY: `cron.schedule` stores its SQL as text and does NOT validate the referenced table at schedule time — it would error on every run until SEC-03 creates the table. Creating a forward-compatible table now keeps ROADMAP Success Criterion #4 truthful exactly as written (all three jobs committed AND valid), and the table is additive/backward-compatible per the CLAUDE.md migration constraint. SEC-03 will extend, not replace, it.

    1. Add (above the pg_cron block, in the Tables section style) `create table if not exists public.csp_violations` with at minimum these forward-compatible columns so the SEC-03 dedup key and the 30-day TTL both work: an `id` primary key (bigint generated always as identity, or text — match project style), `document_uri_path text`, `directive text`, `blocked_host text`, `source_host text`, `count integer not null default 1`, and `created_at timestamptz not null default now()`. Add a `created_at` index for the cleanup job's WHERE clause. Enable RLS with no policies (same pattern as shares/rate_limits — service_role bypasses). Keep it minimal; SEC-03 owns the full ingest schema and may ALTER/extend it.

    2. UNCOMMENT and EXTEND the pg_cron block (replace the three commented `--` lines). It must contain, uncommented and live:
       - `create extension if not exists pg_cron;`
       - `select cron.schedule('cleanup_shares', '0 * * * *', $$delete from public.shares where expires_at < now()$$);`
       - `select cron.schedule('cleanup_rate_limits', '*/30 * * * *', $$delete from public.rate_limits where expires_at < now()$$);`
       - `select cron.schedule('cleanup_csp_violations', '0 3 * * *', $$delete from public.csp_violations where created_at < now() - interval '30 days'$$);`
       Use the exact delete statements from SEC-OPS-01: shares/rate_limits delete `where expires_at < now()`; csp_violations deletes `where created_at < now() - interval '30 days'`. Keep the existing "enable pg_cron in Dashboard → Extensions first" guidance comment, updated to note the SQL now self-enables the extension.

    Preserve the "Safe to re-run" idempotency property: `create extension if not exists`, `create table if not exists`. Note in a comment that `cron.schedule` with the same job name is upsert-like in pg_cron (re-running is safe).
  </action>
  <verify>
    <automated>cd /Users/kaiwu/Desktop/html_to_web_test && body=$(grep -v '^[[:space:]]*--' supabase/schema.sql); echo "$body" | grep -q "create extension if not exists pg_cron" && echo "$body" | grep -q "create table if not exists public.csp_violations" && echo "$body" | grep -q "cleanup_csp_violations" && echo "$body" | grep -q "cleanup_shares" && echo "$body" | grep -q "cleanup_rate_limits" && echo "$body" | grep -q "interval '30 days'" && echo "SCHEMA_OK" || echo "SCHEMA_MISSING"</automated>
  </verify>
  <acceptance_criteria>
    - supabase/schema.sql contains a `create table if not exists public.csp_violations` with at least the columns: document_uri_path, directive, blocked_host, source_host, count, created_at, plus a created_at index and RLS enabled with no policies (grep, after stripping comment lines, confirms the table and an `enable row level security` on it).
    - The pg_cron block is UNCOMMENTED (the grep gate strips `--`-prefixed lines, so the four live statements must survive comment-stripping).
    - All three cron jobs are present and live: cleanup_shares, cleanup_rate_limits, cleanup_csp_violations, with the exact delete statements from SEC-OPS-01.
    - The csp_violations cleanup uses `where created_at < now() - interval '30 days'`.
    - The file remains idempotent (`if not exists` on extension + tables); no destructive DROP statements were added (grep confirms no `drop table`).
  </acceptance_criteria>
  <done>schema.sql commits a forward-compatible csp_violations table and three live cron.schedule jobs; the previously-commented block is now active and the csp_violations job references a real table.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Operator applies schema.sql to production Supabase and confirms pg_cron</name>
  <read_first>
    - supabase/schema.sql (the updated file from Task 1 — this is exactly what the operator runs)
  </read_first>
  <what-built>
    Task 1 committed the pg_cron extension, three cron.schedule cleanup jobs, and a forward-compatible csp_violations table to supabase/schema.sql. This is the SOURCE OF TRUTH but is NOT yet applied to the live database — build and type checks pass without it, which would be a FALSE-POSITIVE for SEC-OPS-01. This task closes that gap.
  </what-built>
  <how-to-verify>
    This step requires the operator because the project has no migration tooling (single supabase/schema.sql) and enabling pg_cron + registering cron jobs is a production database action with elevated privileges that Claude cannot perform autonomously.

    1. Open the Supabase Dashboard for the html2u production project → SQL editor → New query.
    2. Paste the FULL updated contents of supabase/schema.sql and run it. (It is idempotent — safe to re-run.) If the editor rejects `create extension pg_cron` due to permissions, first enable pg_cron under Database → Extensions, then re-run.
    3. Confirm the extension: Database → Extensions shows `pg_cron` ENABLED.
    4. Confirm the jobs: run `select jobname, schedule, command from cron.job order by jobname;` and verify exactly three rows appear: `cleanup_csp_violations`, `cleanup_rate_limits`, `cleanup_shares`, each with the expected delete command.
    5. Confirm the table exists: `select 1 from public.csp_violations limit 1;` returns without a "relation does not exist" error (zero rows is fine).
    Expected outcome: pg_cron enabled, three jobs registered, csp_violations table present so the cleanup_csp_violations job will run without error.
  </how-to-verify>
  <acceptance_criteria>
    - Operator confirms `pg_cron` is ENABLED in Database → Extensions.
    - `select jobname from cron.job` returns the three expected job names.
    - `public.csp_violations` exists in the production project (the cleanup_csp_violations job will not error).
    - Operator pastes the `cron.job` query result into the resume signal / SUMMARY as proof (goal-backward evidence for Success Criterion #4).
  </acceptance_criteria>
  <resume-signal>Type "applied" and paste the `select jobname, schedule from cron.job` result showing the three jobs, or describe the error encountered.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| operator → production Supabase | Privileged SQL execution; enables an extension and registers cron jobs that run with database owner privileges |
| pg_cron scheduler → tables | Cron jobs run autonomously on a schedule with elevated rights; their SQL is fixed text committed in schema.sql |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05 | Elevation of Privilege | pg_cron jobs run with elevated privileges | mitigate | Job SQL is fixed literal text in schema.sql (no dynamic SQL, no user input). The three jobs only DELETE aged rows from three known tables — no DDL, no privilege grants, no cross-schema access. Acceptance asserts the exact delete statements; no `drop` or `grant` present. |
| T-01-06 | Denial of Service | cleanup_csp_violations references a missing table | mitigate | Tension-2 decision: create the csp_violations table in THIS plan so the job never errors per-run. Without it the job would log an error on every 3am run until SEC-03. Acceptance requires the table to exist in production (Task 2 step 5). |
| T-01-07 | Tampering | re-running schema.sql in prod | accept | Idempotent (`if not exists`, cron.schedule upsert by jobname). Re-running cannot duplicate jobs or drop data. No destructive statements added. |
| T-01-08 | Repudiation/Spoofing | false-positive verification (schema committed but not applied) | mitigate | Task 2 is a BLOCKING human-action gate; SEC-OPS-01 is not considered done until the operator pastes `cron.job` proof. Build/type checks alone do NOT satisfy this requirement. |
</threat_model>

<verification>
- grep gate (comment-stripped) confirms the live extension, csp_violations table, and three cron jobs in schema.sql.
- No `drop`/`grant` statements added (grep).
- BLOCKING: operator confirms pg_cron enabled + three jobs in `cron.job` + csp_violations table present in production.
</verification>

<success_criteria>
- SEC-OPS-01: schema.sql commits pg_cron + three cron.schedule jobs (shares, rate_limits, csp_violations); the formerly-commented block is live.
- csp_violations table exists (forward-compatible with SEC-03) so the third job is valid.
- Operator has applied the schema to production and confirmed all three jobs + the extension.
</success_criteria>

<output>
Create `.planning/phases/01-security-headers-foundation/01-02-SUMMARY.md` when done. Record the csp_violations table decision (Tension 2) and the cross-phase note that SEC-03 will extend this table. Include the operator's `cron.job` query result as goal-backward evidence for Success Criterion #4.
</output>
